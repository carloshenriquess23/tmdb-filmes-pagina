import './index.scss';

export default function Header(){

    return(

        <header className='cabecalho-page'>
            <p>Projeto para meu portfolio, veja-o em:</p>
            <a>www.link.com</a>
        </header>
    );
}